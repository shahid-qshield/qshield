<odoo>
    <data noupdate="0">
        <record id="mail_template_of_notify_complete_service" model="mail.template">
            <field name="name">Notification of service complete to account manager</field>
            <field name="subject">${object.name} service ${ctx['service_status']|safe}</field>
            <field name="email_from">${user.email}</field>
            <field name="email_to">${ctx['email']|safe}</field>
            <field name="model_id" ref="ebs_qsheild_mod.model_ebs_mod_service_request"/>
            <field name="auto_delete" eval="False"/>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 12px;">
                        ${object.partner_id.name}, ${object.related_company_ro.name}, ${object.service_type_id.name},
                        ${object.name},${ctx['service_status']|safe}
                    </p>
                </div>
            </field>
        </record>
        <record id="mail_template_of_notify_application_submission_complete" model="mail.template">
            <field name="name">Notification for application submission complete</field>
            <field name="subject">Application submission Workflow for ${object.service_request_id.name} is
                ${object.status}
            </field>
            <field name="email_from">${user.email}</field>
            <field name="email_to">${ctx['email']|safe}</field>
            <field name="model_id" ref="ebs_qsheild_mod.model_ebs_mod_service_request_workflow"/>
            <field name="auto_delete" eval="False"/>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 12px;">
                        ${object.service_request_id.related_company_ro.name} - ${object.service_request_id.name} -
                        ${object.service_request_id.service_type_id.name} -
                        ${object.status}
                    </p>
                </div>
            </field>
        </record>
    </data>
</odoo>